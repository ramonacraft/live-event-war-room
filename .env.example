# Optional: override Release Gate Lab API URL.
# Local dev defaults to Vite proxy /release-gate-api → release-gate-lab.vercel.app/api/runs
# VITE_RELEASE_GATE_API_URL=https://release-gate-lab.vercel.app/api/runs
